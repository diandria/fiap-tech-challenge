apiVersion: v1
kind: Service
metadata:
  name: mongo-headless
  namespace: oficina
  labels:
    app.kubernetes.io/name: mongo
    app.kubernetes.io/part-of: fiap-tech-challenge
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: mongo
  ports:
  - port: 27017
    targetPort: 27017
