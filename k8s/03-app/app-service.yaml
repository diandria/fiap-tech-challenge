apiVersion: v1
kind: Service
metadata:
  name: oficina-service
  namespace: oficina
  labels:
    app.kubernetes.io/name: oficina-app
    app.kubernetes.io/part-of: fiap-tech-challenge
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: oficina-app
  ports:
  - port: 8080
    targetPort: 3000
